<ul>
  <% @posts.each do |post| %>
    <li>
      <p><%= post.user.name %></p>
      <p><%= link_to post.title, post_path(post.id) %></p>
      <%= link_to '削除する', post, method: :delete, data: { confirm: '本当に削除しますか？' } %>
    </li>
  <% end %>
</ul>
