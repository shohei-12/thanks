<%= form_with(url: search_posts_path, html: { method: :get }, local: true, class: 'search-form') do |f| %>
  <%= f.text_field :search, placeholder: 'キーワードを入力' %>

  <button><%= icon('fas', 'search') %></button>
<% end %>
