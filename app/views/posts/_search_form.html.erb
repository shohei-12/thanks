<%= form_with(url: search_posts_path, method: :get, local: true, class: 'search-form') do |f| %>
  <%= f.text_field :search, value: @search_params, placeholder: 'キーワードを入力' %>

  <button><%= icon('fas', 'search') %></button>
<% end %>
