$(".comments").prepend("<%= j(render 'comment', { comment: @comment }) %>");

$(".error-message").html("<%= j(render 'error_messages', { comment: @comment }) %>");

<% if @comment.valid? %>
  $("textarea").val("");
<% end %>
