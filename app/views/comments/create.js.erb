$(".comments").prepend("<%= j(render 'comment', { comment: @comment }) %>");

$(".error-message").html("<%= j(render 'error_message', { comment: @comment }) %>");

<% if @comment.valid? %>
  $("textarea").val("");
<% end %>
